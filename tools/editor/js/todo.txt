completeXMLstringAt - convert to array with Join
return error message from ApkParser
add all needed comments
remove all unneeded logs
add copyright

add price validation to be a number
limit file extension in APK input to .apk
Change local price style to append from both sides
make file input field read only
make APK file required
split JS file to several logical JS files (ZIP logic, XMLWriter logic, etc)
show building progress
make that Build link act like a submit button in form validation, forbid building until all validations are OK
implement add language
add good subtitles to all the existing fields
tabs for description (with HTML, without, with features withour)
add correct validation to all the elements
